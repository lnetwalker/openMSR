README Datei für die Objekt Erkennung.

Bitte auch das pdf ObjectRecognitionUsingLinux.pdf beachten !

Die Objekt Erkennung schreibt die Koordinaten der 3 erkannten Objekte in den DeviceServer auf
Localhost in die IOGroup 2. 
Zum Test der Objekt Erkennung starten Sie den DeviceServer mit der Standard Configuration.
Rufen Sie im Browser die Adresse: http://localhost:10080/analog.html
Setzen Sie jetzt die IO-Group auf 2 und im Eingabefeld: Values read from Device:
erscheinen die Koordinaten der der Objekte als 0,0,x1,y1,x2,y2,x3,y3
Wenn Java erlaubt ist, zeigen die Applets die Koordinaten.

Zur Bedienung des Programmes bitte die PDF Datei beachten.

Sollen die Werte auf einen anderen DeviceServer oder eine andere IOGroup geschrieben werden
müssen Sie die Werte im Quelltext main.cpp ändern und das Programm neu compilieren.

Sollten Sie in der Lage sein, das Programm so zu ändern, dass die Werte als Kommandzeilenparameter
übergeben werden können, freue ich mich auf Ihren Patch
